import 'package:eldorado/src/base/base.dart';
import 'package:flutter/material.dart';

class RegisterEpcScreen extends StatelessWidget {
  const RegisterEpcScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Stack(
        children: [
          Base(),
        ],
      ),
    );
  }
}
